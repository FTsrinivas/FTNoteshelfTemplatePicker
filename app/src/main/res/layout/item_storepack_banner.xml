<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/store_pack_width"
    android:layout_height="wrap_content"
    android:layout_marginRight="@dimen/margin_thirty">

    <TextView
        android:id="@+id/txtTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium"
        android:textColor="#000000"
        android:textSize="@dimen/store_pack_banner_text_size"
        android:textStyle="normal" />

    <TextView
        android:id="@+id/txtSubTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:alpha="0.7"
        android:fontFamily="sans-serif"
        android:textColor="#383838"
        android:textSize="@dimen/store_pack_banner_text_size"
        android:textStyle="normal"
        app:layout_constraintTop_toBottomOf="@+id/txtTitle" />

    <ImageView
        android:id="@+id/imgBanner"
        android:layout_width="@dimen/store_pack_width"
        android:layout_height="@dimen/store_pack_height"
        android:layout_marginTop="@dimen/_12dp"
        android:scaleType="fitXY"
        app:layout_constraintTop_toBottomOf="@+id/txtSubTitle" />

    <TextView
        android:id="@+id/txtGet"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:visibility="gone" />
</androidx.constraintlayout.widget.ConstraintLayout>