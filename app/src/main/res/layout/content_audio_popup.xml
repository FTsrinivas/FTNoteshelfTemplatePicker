<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/audio_pop_up_back_image_view"
            style="@style/DialogNavButton"
            android:src="@drawable/back_dark_chevron" />

        <androidx.appcompat.widget.AppCompatTextView
            android:id="@+id/audio_pop_up_title_text_view"
            style="@style/DialogTitleStyle"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:text="@string/audio_recordings"
            android:textSize="@dimen/new_19sp" />

        <ImageView
            android:id="@+id/audio_pop_up_new_button"
            style="@style/DialogNavButton"
            android:src="@drawable/add_dark" />
    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/audio_pop_up_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
        tools:listitem="@layout/item_audio_recycler_view" />

    <TextView
        android:id="@+id/audio_pop_up_no_audios_text_view"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:text="@string/no_audio_recordings"
        android:textColor="@android:color/black" />
</LinearLayout>